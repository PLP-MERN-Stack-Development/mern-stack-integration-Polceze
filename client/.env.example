# The base URL for the API. This is used in client/src/services/api.js
# During development with the proxy, it will be the dev server URL.
# In production, it will be the deployed backend URL.
VITE_API_URL=/api